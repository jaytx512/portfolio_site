# Public runtime settings (safe to expose to the client)
PUBLIC_SITE_URL=http://localhost:4321

# Analytics (optional)
PUBLIC_PLAUSIBLE_DOMAIN=

# Add private secrets in a real .env file (never commit secrets)
# Example:
# PRIVATE_ANALYTICS_KEY=
